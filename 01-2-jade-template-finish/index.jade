//- 템플릿 상속 & 확장: extends, block
extends parts/_docTemplate

//- 환경설정 변수 덮어쓰기
append config
	-var pageTitle='Jade 템플릿 엔진 실무활용'
	-var metaDesc='Jade 템플릿 엔진 사용법을 설명합니다.'
	-var metaKeywords='Jade, Template, 템플릿, 사용법'

//- 문서 제목 변경설정
//- block pageTitle
//- 	| Jade 템플릿 엔진 실무활용

//- block metatags 뒤에 내용 추가
block append metatags
	//- meta(name="descriptions", content="Jade 템플릿 엔진 사용법을 설명합니다.")
	//- meta(name="keywords", content="Jade, Template, 템플릿, 사용법")

//- block stylesheets 앞에 내용 추가
prepend stylesheets
	//- link(rel="stylesheet", href="css/style.css")

//- block scripts 앞/뒤에 내용 추가
prepend scripts
	script(src="//cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js")

append scripts
	script(src="//cdnjs.cloudflare.com/ajax/libs/detectizr/1.5.0/detectizr.min.js")
	script.
		Modernizr.Detectizr.detect();

//- 슬로건 변경설정
block slogan
	| Jade는 node.js 기반의 템플릿 언어입니다.

//- 콘텐츠 변경설정
block contents
	h2(lang="en") Jade Template
	p  Jade 템플릿은 3가지 기능을 제공합니다.
	ul
		li
			| 포함(
			a(href="http://jade-lang.com/reference/includes/", target="_blank") includes
			|)
		li
			| 확장(
			a(href="http://jade-lang.com/reference/extends/", target="_blank") extends
			|)
		li
			| 상속(
			a(href="http://jade-lang.com/reference/inheritance/", target="_blank") inheritance
			|)